# Host-side process singleton: one store and one pre-execute listener serve
# every session in the process (the sidecar store is cross-session by
# design). Untagged host listeners are admitted into Agent-scoped dispatch,
# so no {global: true} marking is needed.
- insert:
    - id: fs-observation-log
      name: dsh-fs-observation-log
