# Runtime pin: official 0.1.2-alpha.5 + zw.1

Pin `runtime/revision.json` at fork tag `v0.1.2-alpha.5+zw.1` (`aa77a4b147bb19410c3d645811ca54771c93eb98`).

## Official

- **alpha.4** (`dsh-v0.1.2-alpha.4`): `send_message` replaces one-way `report`; model catalog search/filter + profile headers; UI polish; long-session render cost; `web_fetch` default for Python SDK / Headless / ACP / custom profiles; Web PTC drops default `workflow`; **`Session.events` → `seq` / `eventAt()` / `snapshotEvents()`**; **`SessionSeq` vs `SessionLogOffset`**.
- **alpha.5** (`dsh-v0.1.2-alpha.5`): projection-cache cross-version read compat so upgrades from `0.1.1-rc.2` / `0.1.2-alpha.3` do not fail boot or drop session-list titles.

Intel/x64 policy is unchanged (upstream did not ship it).

## Fork

Merge official `dsh-v0.1.2-alpha.5` onto `aka-danielZhang/deepseek-harness` and keep +zw patches. `publish-fork --list --base 0.1.2-alpha.5` is still the same 11 packages. Guard `todo-completion-guard` now reads `snapshotEvents()`.

This Cloud Agent cannot push that fork (GitHub App is not installed there). Replay + conflict table: [`docs/harness-bump/0.1.2-alpha.5/README.md`](../harness-bump/0.1.2-alpha.5/README.md). After the fork PR merges, tag `v0.1.2-alpha.5+zw.1` so npm-release publishes `@crazx/*@0.1.2-alpha.5.zw.1`. Until then `prepare-runtime` fail-louds on missing crazx versions for this base.

## Oh My DSH plugins

- `dsh-thread` host gateway: `session.events` → `session.snapshotEvents()` (alpha.4 compile break). Client half: `ConnectionHandle.api.sessions.create/rename` is gone; continue() talks to `ctx.remote.session` and keeps `agentPreset` on create (Host still requires it at birth). `ClientContext` type-imports stay on last-published `@deepseek-ai/dsh-client-runtime@0.1.1-rc.2` because 0.1.2 deleted that package.
- `dsh-llm` renamed `CallId` → `ToolCallId` (compaction + fs-observation-log tests). Headless `Modal` forbids `closeLabel` (desktop-bridge update dialog). `TokenMeter` now registers `sessionProjections` at construct; engine tests stub that service. `dsh-sandbox` must be the same baseline as `dsh-llm` (`assertNever` left llm; an rc.2 sandbox against alpha.5 llm fails to load).
- Other plugin `@deepseek-ai/dsh-*` exact devDependency pins move to `0.1.2-alpha.5`. `dsh-mcp-settings` keeps the crazx `mcp-client` alias at the last published `0.1.2-alpha.3.zw.2` until `0.1.2-alpha.5.zw.1` exists.
- `scripts/source-deps.mjs` REGISTRY follows the same split.

## Remaining

- Fork tag / `@crazx/*@0.1.2-alpha.5.zw.1` are not on GitHub/npm yet (this agent cannot push `aka-danielZhang/deepseek-harness`). `prepare-runtime` will fail loud until the operator lands [`docs/harness-bump/0.1.2-alpha.5/`](../harness-bump/0.1.2-alpha.5/README.md). Packaged profile smoke CI stays red for the same reason (`git fetch` of `v0.1.2-alpha.5+zw.1`).
- Plugin exact pins keep `cordis@4.0.1` (alpha.5 peers want `^4.0.2`); install warns, typecheck still passes. Not expanded here.
- `dsh-mcp-settings` `pnpm install` prepare (`tsdown` 0.22.2) fails in this environment with `Failed to import module "unrun"` when lifecycle scripts run; its typecheck still needs `plugin:setup` harness project references.
