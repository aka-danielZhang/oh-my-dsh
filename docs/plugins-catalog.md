# 插件目录册（plugins catalog）

> 本文从 AGENTS.md 迁出（2026-09-13 文档瘦身），是插件成员与 desktop-owned 随包清单的**完整版本沿革事实源**。AGENTS.md 只保留一行式 roster；新增/变更插件时更新本文与对应包内 README，AGENTS.md 只需同步一行。

## 成员与版本沿革

> `dsh-usage-stats` 0.1.2 视觉契约于 2026-09-10 回归设置面板既有的卡片式样式：四张独立摘要卡 + 热力图卡 + 多模型折线卡 + 模型质量双柱卡（0.1.3 起图例可切换单指标/双指标）+ 模型用量圆环卡；下文 0.1.1 的“无框分区/堆叠柱/不展示 quality”历史描述以此修订为准。

- **`plugin/<name>/`** —— 可独立安装、独立打 tag 的 DSH 插件包。成员：`dsh-desktop-bridge`（桌面门控：外链路由、原生注意力通知、桌面指示）、`dsh-mcp-settings`（2026-08-19 subtree 迁入）、`dsh-provider-balance`（2026-08-19 subtree 迁入，纯 DOM 注入）、`dsh-reasoning-efforts`（2026-08-20 新写，host-only：给手写 llm-pi-ai 模型补 `reasoningEfforts` 声明；0.2.0 起规则可附带 `compat` 开关（`supportsReasoningEffort`/`thinkingFormat`，zai 系路由自动探测为关、必须显式声明档位才上线），efforts 与 compat 两片按「缺什么补什么」独立判定、目录继承只豁免 efforts 片，契约见包内 README，决策见 `docs/notes/2026-08-20-reasoning-efforts.md` 与 `docs/notes/2026-08-27-reasoning-efforts-compat-fill.md`）、`dsh-web-search-toggle`（2026-08-20 新写，双面：通用设置页「Web Search」开关——DEEPSEEK_API_KEY 状态提示 + home patch 持久化 + Host assembly/guard 跨 Agent Preset 关闭原生 `web_search`，契约见 `docs/notes/2026-08-20-web-search-toggle.md` 与 `docs/notes/2026-08-21-web-search-toggle-preset-scope.md`）、`dsh-compaction-hierarchical`（2026-08-20 新写，host-only：继承 stock compaction 事务，以有界 map-reduce 让小上下文模型压缩大历史；0.1.3 起作为官方 upstream/既有用户 preset 的兼容 Provider 保留，Oh My DSH 默认由 fork stock basic 接管；契约见包内 README，决策见 `docs/notes/2026-08-20-hierarchical-compaction.md` 与 `docs/notes/2026-08-22-stock-hierarchical-compaction.md`）、`dsh-branding`（2026-08-21 新写，browser-only：占用 `sidebar.brand.name` 替换字标为 "Oh My DSH"+"Harness" pill 并重写 document title——**始终挂载、无桌面门控**，终端/浏览器/桌面同一条路；决策见 `docs/notes/2026-08-21-branding-plugin.md`）、`dsh-fs-observation-log`（2026-08-21 新写，host-only：持久化 `fs/observed` evidence 到 `$DSH_HOME/fs-observation-log/`，进程重启/fork 后在 edit/write 前置阶段按「版本 token 仍相等」恢复观察记录，消除 `FS_NOT_OBSERVED` 误伤且不削弱 stock guard——preset 行挂载、patch 刻意为空、零 harness 运行时 import；契约见包内 README，决策见 `docs/notes/2026-08-21-fs-observation-log.md`）、`dsh-model-image-input`（2026-08-21 新写，browser-only：以 dsh-provider-balance 同类纯 DOM 注入姿势，在 stock「设置→模型→供应商→自定义设置」的每条已保存 pi-ai 模型行内插入图片输入按钮；非原生三态菜单（跟随默认/仅文本/文本+图片）固定 196px、向左对齐并按实际高度上下翻转，0.1.2 起写入改走 typed `remote.settings.mutate`（revision fence 保留；上游 0.1.2 移除 `connection.api` 门面，决策见 `docs/notes/2026-09-08-efforts-image-input-remote-settings-migrate.md`）写整组 `models` 数组、即时生效；中英文 aria/action 双锚排除 DeepSeek 卡片，DOM 失配 fail-invisible，client 半零 `@deepseek-ai/*` 值导入；契约见包内 README，决策见 `docs/notes/2026-08-21-dsh-model-image-input.md`）、`dsh-send-while-running`（2026-08-22 新写，browser-only：占用已声明加性槽 `conversation.input.right`（list、replaceRisk none）；0.2.0 起按钮是**补位 Stop**——0.1.2-alpha composer 的 `primaryStops` 加了 `(empty || blocked)` 项，普通会话 running 且草稿有内容时 stock 主按钮保持 Send 且整行无 Stop，本插件在该状态渲染红色 Stop（点击走 stock 同款 `sessions.scope(id).get('conversation').cancel()`，经注册定义的 per-session `inject` 下发、点击时惰性 `ctx.get('sessions')`；0.2.0 定稿为 `ISessions`/`SessionId`/`IConversation` type-only 全链路类型对齐 stock `scopedConversation`，不声明 sessions 硬依赖），草稿清空 stock 主按钮翻回 Stop 即自动退场、永不重复；0.1.x 的孪生 Send 姿态作废（stock 已原生 running 发送，孪生只会重复）；可见性排除 continuable 子会话与 removed；0.1.1 起的全局「任何状态把 Stop 染主题柔和红」recolor 保留（锚 `button:has(> svg > rect)`，本插件按钮在 slot wrapper 内染不进来、自带同款红）；按钮 `order:1` + `:has()` 作用域把 stock 主按钮 `order:2`，卸载即回到 stock 布局，锚点只用 `[data-slot]` 接缝与 `button:last-of-type`（不引 stock CSS-module 类名）；ui-conversation/locale/runtime 仅 type-only import（client bundle 唯一 require 是 react/jsx-runtime，无需 runtime peer 链接），无桌面门控，终端/浏览器/桌面同一条路；契约见包内 README，决策见 `docs/notes/2026-08-22-dsh-send-while-running.md` 与 `docs/notes/2026-09-03-stop-while-running-rework.md`）、`dsh-model-efforts-editor`（2026-08-27 新写，browser-only：dsh-model-image-input 同类纯 DOM 注入姿势，在 stock 模型设置卡每条 pi-ai 模型行内注入推理档位按钮与内联编辑弹层——三态（跟随默认 / 不推理 / 自定义档位+线上值）+ Z.ai compat 勾选（`supportsReasoningEffort`/`thinkingFormat: zai`），0.1.2 起写入改走 typed `remote.settings.mutate`（revision fence 保留；inject 补声明 `remote.settings` 点号子路径，上游 0.1.2 移除 `connection.api` 门面后旧写法「应用」即抛 `Cannot read properties of undefined`，与 image-input 同批迁移，决策见 `docs/notes/2026-09-08-efforts-image-input-remote-settings-migrate.md`）写整组 models 数组即时生效；上游 stock 刻意不提供 provider 级 effort 控件而 composer 选择器又依赖已有声明，本插件补上 per-model 编辑入口；锚定规则与 fail-invisible 边界同 model-image-input；契约见包内 README，决策见 `docs/notes/2026-08-27-dsh-model-efforts-editor.md`）、`dsh-thread`（2026-08-21 新写，preset-owned 双面插件：Thread 模式在依赖前序结果但属于不同阶段/产物的边界准备有界 Handoff Draft，只有直接用户确认才在同一 Workspace 创建承接 Session——0.2.0 起承接会话继承来源会话 preset，`standard-thread` preset 下线，bundle 层全局挂载 `thread_handoff` 工具行并由「设置→通用→Thread」开关（settings 命名空间 `dsh-thread`，默认开）统一门控工具注入与全部 UI；稳定 `threadId` 关联普通 Session，Header 右侧 utility 图标在对话正文右上角切换持久大胶囊浮层（只由该图标关闭；blank 空日志新会话既不渲染入口按钮也不渲染浮层），展示完整关系导航（会话 8 行/页分页并自动跟随当前会话所在页）和按 Session 切换的显式产物列表（产物 5 卡/页分页），无 Thread 时为居中引导空态而非裸段落，且不占用原生 `details`；侧栏经 fork `sidebar.workspaces.sessionListView` 视图环提供「Thread 分组」列表视图；契约见包内 README，决策见 `docs/notes/2026-08-21-session-thread.md`、`docs/notes/2026-08-25-thread-panel-empty-state-pagination.md`、`docs/notes/2026-08-25-thread-toggle-sidebar-grouping.md` 与 `docs/notes/2026-09-01-thread-card-state-and-settings-row.md`；0.3.0-rc.1 起根治「82-byte 标题改名后被判不 pristine」事故——Link 单一 `state` 拆为 target/title/delivery/relation 四组正交持久 checkpoint + 结构化 failure（`recovery` 决定 UI 动作）：目标身份由指纹（`createdAt ≥ 授权`/preset/workspace/cwd，首次 reconcile 钉住、后续 exact match）负责；语义 pristine 忽略任意数量 `session/title`（展示元数据永不鉴权）、外来 user/turn/tool/command/inbox 判 `diverged` 且永不注入；acceptedTitle/eventSeq 只取 rename 响应或对账采纳目标日志当前标题（绝不自拷上游 maxTitleBytes 规则），标题失败/未知不阻断交接、unknown 不自动重试 rename；投递消息 id 先于首次 inbox mutation 落盘（`submitting` checkpoint），崩溃窗口按 id 在场判定——双在补提交、缺失者以新 id 重投、flush 未知 → `uncertain` 需用户确认 redeliver（自检只补缺失）；UI 假「重试」删除，改真实动作（继续创建/重新检查/启动交接/打开已创建会话/克隆到新会话/重新投递/取消授权），CAS 类错误由 Client 自动重读 Link 显示「状态已更新」；存量 Link 在 zod parse 边界 `z.preprocess` 迁移 + boot 一次性清 `legacy` capture 重写（domain 不 bump version），事故形态（failed + title-only pristine）→ `published`+title `unknown` 等待再确认，真 diverged 保留普通 Session 并可克隆（新 Draft/Link/target）；顺带把 gateway 5 处 `snapshotEvents()` 对齐 0.1.5-alpha.1 的 `Session.events`（rc.7 源码对当前依赖过不了 typecheck），决策见 `docs/notes/2026-09-10-thread-title-saga-checkpoints.md`）、`dsh-ohmymemo`（2026-09-03 新写，双面：OhMyMemo 用户长期记忆——`$DSH_HOME/ohmymemo` 一条记忆一个 `.md` + YAML frontmatter，含 manifest/config 与完整 schema 解析、进程内目录（重复 ID/single-key 冲突 fail closed 隔离）、watch/doctor、原子发布（临时文件+fsync+rename+目录 fsync，0600/0700）、Store 级跨进程写锁（mkdir 原子抢占，pid+进程启动标识判定陈旧、绝不按时间回收，等待有界返回 OHMYMEMO_BUSY）、revision+hash 双重 CAS（记忆修订 ifHash 必填、memory_get 返回 hash；外部手编以磁盘为准并 journal `external-edit-detected`，同 revision 手编不会被陈旧调用覆盖）、tombstone 读取屏障（memory_ids 屏蔽 search/get/capsule/浏览器，延迟删除残留不可召回而 doctor 仍可见）、写锁内重读 config.yaml 防陈旧策略、纯元数据 journal 与事务标记前滚恢复（forget 的 tombstone 先行屏障；恢复按 hash 判定，不猜时间；marker 按不可信输入严格校验——schema/ID-文件名绑定/路径 allowlist/symlink 拒绝，before_hash 竞态零写入中止不留冲突标记；close() 不强释锁，在飞回调自释、进程退出走陈旧判定）、user/workspace scope 解析（realpath 关联 `ws_<ulid>`，dsh workspace id 优先）；Phase 2（同日）在同一包补齐显式记忆闭环：`ohmymemo-store` 行 provide `ctx.ohMyMemo` 服务并确定性重建 `views/`，`ohmymemo-tools` 行（peer 依赖钉运行时副本防模块分裂）挂五个 `memory_*` Tools + 准入提示段（来源绑定 exec.agent，subagent 只读、写拒绝，凭据 fail closed），`ohmymemo-context` 行在 `agent/pre-step` 以持久 `user/message` 注入有界记忆 capsule——权限声明前置、digest 对账（同 digest 跳过、变更注入显式替换消息、resume/重启经 surface 回扫一致），预算按 workspace→confirmed→pinned→importance 确定性截断；2026-09-04 增加默认关闭的 Host 本地每日梦境提取（只读 append-origin direct-human 消息、受限无 Tool maintenance Agent、strict JSON + 精确 evidence、只写 sensitive candidate）、跨进程 maintenance lease、持久 Run/增量 cursor/Jobs 取消，以及顶级“记忆”设置中的状态控制和 allowlist 只读 Markdown tree/viewer（desktop 双栏、narrow 列表/正文返回）；契约见包内 README，设计见 `docs/notes/2026-09-03-ohmymemo-memory.md`，实现决策见 `docs/notes/2026-09-03-ohmymemo-store-phase1.md`、`docs/notes/2026-09-03-ohmymemo-phase2.md` 与 `docs/notes/2026-09-04-ohmymemo-dream-memory-and-ui.md`；0.1.0 起标记 `dsh.desktop.ship` 随桌面发货——`ohmymemo.tar.gz` → `~/.dsh-desktop/plugins/dsh-ohmymemo/`，纳入首启 Profile 安装事务，tarball hash/版本进 revision manifest（`ohmymemoTarball`/`ohmymemoVersion`），发货决策见 `docs/notes/2026-09-05-ohmymemo-desktop-ship.md`；2026-09-08 补齐记忆生命周期（0.2.0）：读时衰减——capsule 改按 `importance × decay(age, H)`（age 以 `last_evidenced_at ?? created_at` 为基准，刻意不用 `updated_at`；H/2 全额→H 降 0.2→2H 归零；confirmed ≡ 1）排序与零权重剔除，`valid_until`/`valid_from` 检查进 `isCoreViewEntry(entry, now)` 共用闸门（capsule 与 views 同源翻转），水位线走 config `decay_horizon_days_semantic/procedural/episodic`（默认 365/180/90，逐字段容错回退），零写盘；夜间确定性维护段——dream run 提取段后插入（无新证据也执行）：候选 `candidate_expires_at` 已过确定性删除（journal `candidate-expired`，补齐设计既定语义），`valid_until` 已过或未 confirmed 沉默 ≥ 2H 的 active 记录归档 `status: expired` + `archive/`（`expireLocked` 复用 supersede 的 write-derived 事务机器、崩溃可前滚，journal `memory-expired`），归档**绝不走 tombstone** 且 `hasMemoryKey`/候选查重收窄到 active/disputed——同 key 重新入库不被旧归档挡死，`memory_get` 按 id 仍可读归档；Curator（`auto_consolidation` 门控，默认关）——同一维护 Agent 第二个 followup turn，输入 active 目录元数据 NDJSON（active+normal+未 confirmed，weight 升序最老先看，截断 `curatorMaxEntries`）+ 与提取器同一证据窗口，refresh 提议须 quote 精确子串 grounding、merge 双方须同 scope+kind 且在目录（confirmed/sensitive 不进目录、不可触碰），merge 走 supersede 机器（absorbed 归档 + `supersedes` 回链，journal `memory-merged`）、refresh 原地修订刷 `last_evidenced_at`（journal `memory-refreshed`），unsure 一律 keep，Curator 失败=部分成功记审计 detail、不毒化提取器死信连胜；提取 prompt 增可选 `valid_until`（仅时间性事实）透传入库，`memory_remember` 增可选 `validUntil`（裸日期展开当日 UTC 结尾）；audit/summary 新增 expired/curator 计数均 `.default()` 后兼容；设置页计数与状态 chip 增 `expired`；决策见 `docs/notes/2026-09-08-ohmymemo-lifecycle.md`；2026-09-09 修视图稳定性（0.2.2）：`rebuildViews` 落盘前与磁盘内容做「掩去 `generated_at` 行」的等值比对——配置更新等无数据变化的订阅触发不再重写视图文件，`generated_at`/文件 hash/浏览器目录 generation 全部稳定（此前记忆设置页连续 `config-updated` 即把每个打开页面的目录打成陈旧、点开文件报 `OHMYMEMO_TREE_STALE` 内部错误）；Client `read()` 遇 `OHMYMEMO_TREE_STALE`（目录 generation 过期与文件 hash 变动两处守卫）自动重取目录、按新 generation 重试一次、成功后采纳新目录，仍失败才上报；`unwrap` 抛错改带 `code` 属性支撑分支判断，决策见 `docs/notes/2026-09-09-ohmymemo-view-stability.md`；2026-09-09 增设 Beta 预览角标（0.2.3）——「设置→记忆」标题旁的小 pill（locale 键 `betaBadge`，样式只用 `--dsw-*` token），2026-09-12 修 memory_search 输出契约（0.2.4）：hit items schema 补声明 `score`（实现自 Phase 2 起恒带、schema 漏声明，`additionalProperties:false` 下 ToolRegistry 输出校验 fail loud，凡命中 ≥1 即报 invalid output——空结果可过校验、记忆主链路不走工具校验缝，故上线九日才首爆）；`tests/tools.test.ts` 新增契约测试，把五个 memory_* 工具真实 `execute()` 返回值过 dsh-tools 公开导出的 `validateJsonSchemaValue`（复现 `createSuccessResult` 校验缝——mock registry 直调 execute 绕过该缝正是漏网根因），决策见 `docs/notes/2026-09-12-ohmymemo-search-output-schema.md`；2026-09-12 交互面索引优先重构（0.3.0）：读路径全面文件化——`views/` 新增 Agent 全量索引 `views/index-user.md` 与 `views/index-workspace-<ws>.md`（收录 active+normal+未 quarantine+validity window 内**全部**记忆、不要求 pinned，decayWeight 降序，行数上限 config `index_max_entries` 默认 200、超出截断并脚注计数；行格式 `[id] (kind · key · importance X) 摘要 → scopes/…/mem_x.md`，与 capsule top 行共用 `composeIndexLine`；人读 pinned 视图与浏览器记忆树不变），capsule 从「正文全量注入」改为「索引指针 + 每作用域 top `capsule_top_entries`（默认 5）一行摘要 + Store 根路径指引」，decay 三函数下沉 `src/decay.ts` 供 capsule/views 共用；`memory_search`/`memory_get` 从模型工具面**退役**（工具面收敛为 remember/update/forget 三个写工具——凭据拦截、subagent 只读门控、single-key 冲突、supersede/dispute 语义全部保留在写缝上；service 层 search/get 方法保留供 dream/curator/UI 内部使用），`memory_remember` 的 tags 宽容化（尽力规范化、不可规范化字符如中文直接丢弃而非硬失败，全空等同未传），`memory_update` 的 `ifHash` 降为选填（`ifRevision` 必填、从记忆文件 frontmatter 读取，Store 锁内以磁盘重读为准，显式携带 hash 时才校验），新增 config `capsule_top_entries`/`index_entry_summary_chars`/`index_max_entries`（默认 5/120/200，逐字段容错回退）；写后震荡消除——capsule 注入决策只在 turn 首步做（turn 中途 digest 变化推迟到下一 turn 首步对账），写工具成功后经 service 的 per-session 有界豁免集记录「本次自写产生的 capsule digest」，对账命中豁免只更新缓存不注入（跨会话/外部写入仍替换注入）；0.2.x 存量库零迁移（schema/文件格式/manifest 未动）；sensitive 文件**维持原位**不挪 `scopes-private/`——挪目录即改 Store 布局内核、违反「Store 内核零改动/零迁移」硬约束，且索引/capsule/UI 本就不收 sensitive、grep 全库语义不变，GUIDANCE 与索引头注明「除非用户明确要求，不要主动读取 sensitive 正文」；写工具彻底文件化（inbox/*.md 由 watch 校验入库）明确留作下一阶段；决策与落地偏差见 `docs/notes/2026-09-12-ohmymemo-index-first-interaction.md`；2026-09-13 每日时间改双列滚轮（0.3.1）——设置页「梦境记忆·每日时间」的原生 `<input type="time">` 换成与定时任务一致的双列滚轮下拉（小时/分钟等宽两列、隐藏滚动条、打开时当前值居中），触发器内部以与菜单相同的两列几何渲染（同 padding/gap、各 `flex:1`），冒号绝对定位正中，框内数字与两列中心精确对齐）、声明记忆功能当前为预览版）、`dsh-usage-stats`（2026-09-10 新写，双面三行：设置一级页「使用统计」——collector 行 fold `assistant/message` 的 usage（live `session/event` + 启动 `sessionQuery.readSession` 回填，`message.id` 跨会话去重坍缩 fork 种子与双进程竞态）落 `$DSH_HOME/usage-stats/records/` 日滚 JSONL（**records 是唯一事实源**，启动全量重建去重表与聚合，`aggregates.json` 仅 advisory；gateway 查询前 rescan 字节水位增量收编其他进程 append 的行，共享 Home 无锁 last-wins 下永不重不漏）；gateway 行 Typert Remote `usageStats` 四查询（summary/daily/activity/breakdown）下发聚合纯 JSON；主行 typert strict 注册；client 半 `settings.section` 注册 + 手写 SVG 图表（0.1.1 视觉重构：单列统计面板——摘要带（唯一带边框容器，容器查询 680/520/420px → 4 列/2×2/单列）、52 周×7 天 8px 格热力图 daily/weekly（累计模式撤下 UI、Remote 保留兼容）、按日堆叠柱趋势（柱总高=当日总 Token，Top 5+其他）、模型用量圆环+排行同源恒合计 100%；无第三方图表库，全部 className 走 CSS Modules 映射、数据系列色集中组件级 `--us-*` 变量指向 `--dsw-static-*`，CSS Modules 预打 data-plugin 标记，`ctx.locale` 中英双语中文优先、formatter 双 profile（万/亿 vs K/M/B、本地日历日期），请求分 summary/activity/range 三组各持 generation guard（快速切换旧响应丢弃、局部失败就地重试），筛选/热力图键盘游标/Tooltip 全可键盘访问，新鲜度=挂载拉取+手动刷新（完成时 role=status 播报一次）+focus 重拉；四项摘要=累计/平均缓存命中（cr÷计费输入）/平均输出速度（output÷调用端到端时长）/平均调用时长（step 起点→回复），峰值与连续天数字段保留在 wire 不上卡；连续天数按本地时区（今天无数据不清零）、失败/取消/无 usage 调用不计、内部调用（llm/stream）v1 不计、不做费用估算；**0.1.5 日志无流式增量（attempt 稀疏），速度/首token 只能端到端口径**；时间范围独立过滤行仅驱动趋势+模型用量（from/to ≤120 天，无效日期就地报错不发请求）；回填注意：新版 harness 会话日志为 `session.v3.jsonl.zstd`，旧基线 runtime 的 persistence 不认该文件名——v3-only 会话从 `listSessions` 静默消失，验证/部署必须用与写日志同代 runtime；契约见包内 README，设计见 `docs/notes/2026-09-09-usage-stats.md`，实施决策见 `docs/notes/2026-09-10-usage-stats-impl.md`；rc.45 起标记 `dsh.desktop.ship` 随桌面发货——`usage-stats.tar.gz` → `~/.dsh-desktop/plugins/dsh-usage-stats/`，纳入首启 Profile 安装事务，tarball hash/版本进 revision manifest）、`dsh-copy-session-id`（2026-09-11 新写，browser-only：接管会话头部「⋯」菜单——以 `priority: -1` 抢占官方 `session-log-export` 在加性槽 `conversation.session.header.utilities` 的 `session-log-download` cell（list cell 同 id 同 priority 是硬错误，priority 升序最小者渲染；被压条目留在 ledger、本 fiber dispose 后自动回归，卸载即恢复原样），菜单首项新增「复制会话 ID」（`navigator.clipboard.writeText` → 隐藏 textarea `execCommand('copy')` 两级回退，1.6s ✓ 反馈；会话 id 来自注册 inject 工厂下发的 `sessionId`，组件 props 不含它），并重渲染被顶掉的「下载 Session 日志」项与下载状态弹层（经 cordis 服务 `sessionLogDownload` 结构鸭子校验消费——`store.getSnapshot/subscribe` + `download/dismiss`，控制器一律调用时惰性 `ctx.get`，服务缺席或形状漂移降级为仅复制菜单、官方 `/export` 命令触发的状态同弹层显示），样式预打 `data-plugin`/`data-plugin-css` 去重、只用自有 `.dsh-csid-*` 命名空间与 `--dsw-alias-*` token（「⋯」锚点照抄官方 `.moreButton` 28px/15px glyph 保证像素中性），时延走 Cordis `timer`（`ctx.timer.timeout`）不用裸定时器；零 `@deepseek-ai/*` 值导入（唯一 require 是 react/jsx-runtime）、无桌面门控，终端/浏览器/桌面同一条路；契约见包内 README，决策见 `docs/notes/2026-09-11-dsh-copy-session-id.md`）、`dsh-scheduled-tasks`（2026-09-11 新写，双面三行：入口是侧栏会话区标题行里、搜索按钮左侧的时钟按钮（原「设置→定时任务」一级菜单于同日二次评审撤除），切到 layout keyed `main` 座 `automation` 面板、再点同一个按钮回会话——core 行 provide `scheduledTasks` 服务：Storage Domain `dsh_scheduled_tasks`（tasks/runtime/runs 三表 v1 strict）上的用户任务 CRUD（title ≤100、instruction ≤8000、五类调度（hourly 第 N 分 / daily / weekdays / weekly ISO 周几 / monthly 号数，墙钟严格 HH:mm，创建时钉住 IANA 时区；`daily` 成员字段集与扩展前一致，故存量 v1 记录免迁移、domain 不 bump version）、≤100 个任务、全部 mutation 带 ifRevision CAS，冲突返回 `TASK_REVISION_CONFLICT` 让 Client 重读）+ 全局调度器（单 one-shot timer 指向最早边界；五类规则共用一套固定 IANA 时区边界走子（两次 offset 迭代；fall-back 取较早 instant、spring-forward gap 顺延至跳变后等价分钟；hourly 在 gap 塌陷处与 overlap 重复小时都只触发一次，monthly 遇当月无此日期整月跳过、不折月末）；12h 窗口内只补最近漏跑边界，暂停期不补；每任务跨进程目录锁（pid+启动标识+nonce，绝不按时间偷锁，抢锁失败=边界留给持有者）；fence + commitIntent（claimed → session-created → terminal）崩溃恢复——claimed/session-created 孤儿结算 interrupted 且绝不重放已消费边界，terminal 意图幂等前滚；同任务重叠写 `skipped` audit 不排队；全局并发上限默认 2、结算后 wake 重捡即有界队列）+ managed Provider registry（effect-owned `registerManaged`，单 Provider 失败只降级自身行为 `unavailable`）；run 事务照抄 webhook：预验证 workspace/preset/permission/route（`llm.resolveCallConfig` fail loud 不静默换）→ `agents.create` setup 内 mount preset → attachSession → permissionPresets.set → sessionTitle.rename `[定时] <标题> · <scheduleLabel>` → plugin-origin 消息（source.kind 为 scheduled-task，经 `MessageSourceMap` declare module 扩展，纯类型侧不改 fork）提交 instruction；sessionId 在 claim 时预生成写 audit，崩溃任意点可归因；instruction 不进日志/Job label/错误 detail；执行预设固定 `standard`——Client 请求恒带、UI 不提供预设选择，策略单点在 Client）。client 半注册 keyed `main` 面板 + 侧栏时钟入口（会话浏览区标题行没有 action seat，故以自有 `dsh-stask-entry` class 装饰注入：中英双语 aria 锚、只 append 到行末、MutationObserver+resize 每轮重算 `getBoundingClientRect` 定位并实测该行两个 stock 控件的间隙做等距、锚点缺失即移除按钮退场、卸载还原行的 position；按下态读页面自己发布的 `data-dsh-stask-panel` 标记），编辑器整体对齐 composer（调度行 + 与触发器等宽的双列时间滚轮、22px 胶囊指令框 auto-grow 36/340 + 底栏 `.row` 芯片：工作区 = hero WorkspaceChip + 目录菜单含「添加工作区…」、权限 = composer 芯片 + stock 盾牌三态字形与词表、模型 = composer 芯片 + 两级菜单「模型/推理等级」触发面「模型 · Default」；所有下拉共用 stock Menu 卡片，图标取 `ui-primitives`、文案走 `ctx.locale` 中英双语、颜色只用 `--dsw-*` token；任务列表为整宽单列卡片，操作收进右上「⋯」菜单（立即运行/暂停·恢复/编辑/红色删除），刷新与创建用 stock `Button` 原语）；`dsh.client.inject` 由 `dsh-client-ui-settings` 换成 `dsh-client-ui-layout`（面板与 `ctx.layout.selectPanel` 的类型来源，devDep 钉 0.1.5-rc.2 + peer range 同步）。ohmymemo 行只读投影「梦境记忆」（`system.ohmymemo.dream`，仅调 `ohMyMemoUi.overview()`，enabled→运行中/已暂停、running→执行中，`timeZonePolicy: host-local` 如实标注；**fail soft**——不硬 inject `ohMyMemoUi`，optional 读取 + 1s 有界轮询 60s 放弃，因本仓 runtime 对 pending entry fail loud 会拖垮整个 profile 启动）；api 行 typert strict 注册（scheduledTasks 九方法，含 `runNow`=manual 触发立即跑（暂停中可跑、运行中 `TASK_BUSY`、不推进调度游标）与 `listRuns`/`deleteRun` 运行历史；managed id 一律 `TASK_MANAGED_READ_ONLY`）；执行模型为**一个任务绑定一个长期 session**——首跑创建（webhook 事务），后续 `agents.get` 复用或 `agents.resume` 恢复后追加本轮指令，run 结束仅 dispose 本轮 handle；`runtime.sessionId` 首 claim 绑定（存量免迁移）；工作区创建后锁定（Host 拒绝变更 + 编辑页芯片禁用）；编辑页「设置/历史」页签，历史页为运行记录表（触发时间/来源/状态/时长 + 行内跳到会话/删除记录）。数据不在 settings.yaml；执行中禁编辑/删除（`TASK_BUSY`）、暂停不中断在跑 run、失败不自动暂停；waiting-input 检测（approval 事件对扫描）首版未做，UI 词汇表已支持；依赖教训：npm 副本 `dsh-storage-domain` 的 peer `dsh-storage`/`dsh-invariants` 必须显式 devDep（Node ESM 按导入路径解析，autoInstallPeers off）；契约见包内 README，设计见 `docs/notes/2026-09-11-scheduled-tasks.md`，实现决策见 `docs/notes/2026-09-11-dsh-scheduled-tasks.md`（二次改版见设计 §18 与实现记录「二次改版」节））、`dsh-settings-icons`（2026-09-11 新写，browser-only：把设置导航里三个「兜底齿轮」换成各自语义的线性图标——`usage-stats`/`mcp`/`memory` 三个 `settings.section` id 各配一枚 16×16 描边 glyph（三柱+基线 / 双插脚插头 / 左右脑），以 data-URI `mask-image` 画在 stock 已挂载的 glyph 元素上（`background-color:currentColor`，前缀与非前缀两套 mask 属性并写），不换 DOM 节点、不碰主题 token；stock 设置壳的 `navIcon(id)` 只认 models/agent-presets/plugins、其余一律兜底齿轮，且 `settings.section` 注册面没有 icon 选项，故只能 DOM 装饰；锚点是**与壳同源的 ledger**——按 `ctx.slots.getVersion/entries('settings.section')` 取 id→**实时 label**，再以文本匹配 nav 按钮（两侧 trim、`claimed` 集合防撞文案），标签对不上就整条不画（fail-invisible，绝不错画别行），locale 变更随 ledger 版本自动重取、零硬编码文案；标记 `data-dsh-sicons` 打在 **glyph 元素自身**（React 换节点后标记随旧节点消失，下一次扫描自动重画；若只打父按钮会出现「样式被换掉却再也不重画」的静默失效），写入逐项记账、卸载精确还原 `style`/`display` 并剪掉已被换掉的旧节点；0.1.1 起重画收敛进 MutationObserver 回调（微任务先于浏览器绘制，修「每次打开设置先闪一帧齿轮」），`ctx.timer` 依赖随之移除；client bundle 零 `@deepseek-ai/*` 值导入（无 require、无需 runtime peer 链接），无桌面门控，终端/浏览器/桌面同一条路；契约见包内 README，决策见 `docs/notes/2026-09-11-dsh-settings-icons.md`）

## desktop-owned 随包清单（壳 release 携带集）

- 壳的 release 只携带其运行面直接依赖的桌面自有插件，不按 `plugin/*` 无差别收包。当前集合是 bridge 0.2.0-rc.18（`bridge.tar.gz` → `~/.dsh-desktop/bridge/`；0.2.0-rc.3 起标题栏融合 CSS 首条规则 `html,body{overflow:hidden;}` 锁死根滚动；0.2.0-rc.4 起安装确认框展示 CHANGELOG 抽取的更新说明；0.2.0-rc.5 起 IPC 载体为 `window.__DSH_DESKTOP_IPC__`，兼容归档 Tauri 的 `__TAURI_INTERNALS__`；0.2.0-rc.6 起系统通知带 sessionId，点击回跳 `sessions.open`；0.2.0-rc.7 起新建/打开会话的 agent 附着脉冲不出 turn-done；0.2.0-rc.8 起更新入口改为点击弹窗下载——进度条 + 取消下载（新增 `dsh_desktop_cancel_update` IPC）+ ready 后「稍后 / 重启以更新」，不再发现即后台自动下载，决策见 `docs/notes/2026-08-28-update-download-dialog.md`；0.2.0-rc.9 起弹窗图标块为静态 app logo（`update-logo.tsx`），旋转只留在带内按钮；0.2.0-rc.10 起手写 `<style>` 注入预打 `data-plugin`/`data-plugin-css` 并做插入幂等，防 client-modules `claimStyles` 跨插件误认领后被认领方 HMR rebuilt 连坐删除（2026-09-08 rail/titlebar 样式消失事故，决策见 `docs/notes/2026-09-08-style-tag-claiming-hmr.md`）；同版壳侧修复更新退出序列 veto 并加 slim 运行时预置，决策见 `docs/notes/2026-08-31-update-restart-fix.md`；0.2.0-rc.11 起整宽拖拽条改为分段拖拽条（`drag-strip.ts`：给 `[data-sidebar-right-panel]` 带内按钮挖洞的间隙段，`app-region:drag` 只在段上），并以 `[data-sidebar-right-panel]{top:28px!important}` 把绝对定位的右侧栏面板顶边压到标题带下——修 0.1.5-alpha.1 运行时右侧栏页签条被拖拽条吞点击/悬停、最大化时撞红绿灯，决策见 `docs/notes/2026-09-09-rightbar-band-drag-segments.md`；0.2.0-rc.12 起内容顶到窗口上沿消灭 28px 空带——栏列 padding 只留首栏（侧栏内容让红绿灯），中间/右栏内容顶到 y=0、session header 与右栏页签条进顶部带成 macOS 工具栏式布局；右栏面板按模式分流：push/float 顶 y=0 靠拖拽段挖洞保护；fullscreen 曾两易其稿——rc.11 无条件 `[data-sidebar-right-panel]{top:28px!important}`、rc.12 收窄为 fullscreen 限定，0.2.0-rc.13 起连这条也删：实机否决（面板掉第二行与仍露出的中间栏 header 叠看，不符全屏语义），fullscreen 恢复原生 `fixed inset:0` 真接管全窗，首个 pane 的 `[data-dockkit-strip]` 加 `padding-left:80px` 让灯排、strip 背景带 `-webkit-app-region:drag`（内含 button/`a[href]`/`[role=button]`/`[role=tab]`/input/textarea/contenteditable 一律 no-drag）——z-40 盖住 overlay 层后拖拽段够不到带，拖拽面交给页签条；fullscreen 期间 rail 控件（侧栏开关/通知/新会话/更新入口）被面板盖住不可用，专注态取舍、退出走面板自己的按钮；挖洞放宽为全文档压带可交互元素（header 的 utilities/tabs 等，防重蹈整宽条吞点击）；收起态给 `[data-slot="conversation.session.header"]` 加 `padding-left:80px` 让出灯排（与 rail 控件 `left:86px` 同标线），决策见 `docs/notes/2026-09-09-band-content-top-design.md`；0.2.0-rc.14 起标题带重构为**统一 toolbar**（同日撤销首版的动态避让方案，见 `docs/notes/2026-09-10-collapsed-header-clears-rail-controls.md` 撤销记录与 `docs/notes/2026-09-10-unified-shell-toolbar.md`）：fork `ui-layout` 新增 `shell.toolbar` Slot（single、session-maybe）且 AppFrame 改两行 grid（`auto minmax(0,1fr)`，无占用者首行 0 高、普通 Web/Win/Linux 逐像素不变），桥以 `desktop-toolbar` 条目独占——38px 单行（中线 y19 与灯排同线）、Leading 86px 灯区+侧栏开关+会话后退/前进+新会话、Center 双 portal host（session header 的 title/actions/utilities 投入 centerHost，rightbar corner 投入 sessionEndHost 保持最右）+Workspace pill、Trailing 运行面/更新/通知；根背景 `-webkit-app-region:drag`、交互子元素 no-drag，**普通模式分段拖拽条整体删除**；rc.11–rc.13 的「内容顶到上沿+挖洞+动态避让」补丁链随之作废（仅作历史），旧规则以 `html:not([data-desktop-toolbar])` 门控保留为降级 fallback（降级态=rc.13 行为），fullscreen 语义不变；会话后退/前进来自 fork session-controller 的有界 transient selection history（`ISessions.canBack/canForward/back/forward`：连续重复去重、新选择截断 forward、已删会话从历史剔除、clear 态入史），bridge 端全部结构化 duck-check，旧 runtime 上按钮禁用；**0.2.0-rc.15 起槽注册本身防御**——`shell.toolbar` 注册包 try/catch，旧 runtime（SlotMap 无此槽）上注册失败仅 warn 降级：无工具栏、`data-shell-toolbar-on` 标记不设置（titlebar.ts 的 rc.13 fallback 规则保持生效）、桥其余能力全部存活——修复 rc.45 事故（rc.14 无防御注册在 zw.1/zw.2 runtime 上 fail loud，整桥加载失败且更新入口随之失效，无法自愈；根因之二：发版时 revision.json 未同步指向含 `shell.toolbar` 基座的 fork，rc.46 起钉 `v0.1.5-alpha.1+zw.3`，教训见下条纪律）；0.2.0-rc.16 起按实机验收收敛 toolbar：删除 Back/Forward、Workspace pill 与 toolbar 内运行面图标；controls 改为 in-flow（侧栏开关 / 条件更新 / 通知 / 仅收起态新会话），main 以 subgrid 对齐会话列并按 176/204px 实际控件宽度避让；Session Header 分为左侧标题/动作簇与最右 Thread/rightbar 簇，内容间距 8px、两侧工具图标间距 2px，交互后代 `no-drag!important` 且 main 盒做 pointer-events 分区，决策见 `docs/notes/2026-09-10-desktop-toolbar-layout-correction.md`；0.2.0-rc.17 曾试验「更新控件常驻 + 显式确认下载」（idle/current 恒渲染检查 glyph，available 态弹窗确认后才下载），同日按用户评审撤销——rc.18 回归 rc.16 的条件可见形态与点击即弹窗下载流程（代码与 rc.16 相同，仅版本号走动），撤销决策见 `docs/notes/2026-09-12-update-control-manual-check.md`），层次压缩兼容 Provider 0.1.3（`compaction-hierarchical.tar.gz` → `~/.dsh-desktop/plugins/dsh-compaction-hierarchical/`）、Web Search 开关 0.1.4（`web-search-toggle.tar.gz` → `~/.dsh-desktop/plugins/dsh-web-search-toggle/`）、模型图片输入 0.1.3（`model-image-input.tar.gz` → `~/.dsh-desktop/plugins/dsh-model-image-input/`；无需 runtime peer 链接——client bundle 零 `@deepseek-ai/*` 值导入、host 半空 apply；0.1.2 起写入改走 typed `remote.settings.mutate`——上游 0.1.2 移除 `connection.api` 门面，与档位编辑器同批迁移，决策见 `docs/notes/2026-09-08-efforts-image-input-remote-settings-migrate.md`）与运行中补位 Stop 0.2.1（`send-while-running.tar.gz` → `~/.dsh-desktop/plugins/dsh-send-while-running/`；同为零值导入的 browser-only 形态、无 peer 链接；0.1.1 起 Stop 按钮任何状态染主题柔和红，与蓝 Send 视觉区分；0.2.0 起适配 0.1.2-alpha composer——`primaryStops` 加 `(empty || blocked)` 后普通会话 running+有草稿时整行无 Stop，插件在该状态渲染补位红色 Stop、点击走 stock 同款 `sessions.scope(id).get('conversation').cancel()`，孪生 Send 姿态作废，决策见 `docs/notes/2026-09-03-stop-while-running-rework.md`；0.2.1 起样式表注入预打 data-plugin 标记防 HMR 跨插件误删，同 `docs/notes/2026-09-08-style-tag-claiming-hmr.md`）、模型档位编辑器 0.1.4（`model-efforts-editor.tar.gz` → `~/.dsh-desktop/plugins/dsh-model-efforts-editor/`；同为零值导入的 browser-only 形态、无需 peer 链接——dsh-model-image-input 同类纯 DOM 注入，在 stock 模型设置卡每条 pi-ai 模型行内提供推理档位内联编辑：三态跟随默认/不推理/自定义档位+线上值 + Z.ai compat 勾选；0.1.2 起写入改走 typed `remote.settings.mutate`（inject 补声明 `remote.settings` 点号子路径）——上游 0.1.2 移除 `connection.api` 门面，0.1.1 的「应用」会报 `Cannot read properties of undefined (reading 'settings')`，与 image-input 同批迁移，决策见 `docs/notes/2026-09-08-efforts-image-input-remote-settings-migrate.md`；0.1.4 起样式表注入预打 data-plugin 标记防 HMR 跨插件误删，同 `docs/notes/2026-09-08-style-tag-claiming-hmr.md`）与 Thread 交接 0.3.0-rc.1（`thread.tar.gz` → `~/.dsh-desktop/plugins/dsh-thread/`；显式跨会话 Handoff 双面插件：bundle 层全局挂载 `thread_handoff` 工具 + 通用设置总开关（settings 命名空间 `dsh-thread`，默认开）+ 看板/侧栏分组/卡片 UI；host 半按 package.json `dependencies`/`peerDependencies` 链到 assembled runtime（缺 dependency 即 fail loud）防模块身份分裂；0.2.0-rc.3 起 host bundle 内联 zod（zod 归 devDeps + tsdown `deps.onlyBundle`，web-search-toggle 同款）——桌面解包姿态无 node_modules、仅有该链接表，裸 `zod` import 会让 loader entry 全挂、boot 即死（rc.23/rc.24 无法启动的根因），且 typert codec 走 duck check、内联副本合法；0.2.0-rc.4 起对齐 0.1.2 删除的 `settingsNamespace()`——`register` 直接收命名空间字符串（wst 0.1.4 同款；rc.25/rc.26 仍无法启动的第二层根因，rc.27 才是 Thread 进包后首个可启动版，rc.23–rc.26 用户均需手动下载）；0.2.0-rc.5 起适配 0.1.2-alpha.3 运行时——上游移除 `connection.api` 门面后，创建/改名改走 typed Remote `ctx.remote.session.create/rename`（outward `ctx.sessions.create` 仍不收 `agentPreset`，不满足 preset 继承契约），`dsh-client-runtime` 下线涉及的 `ISessions`/`SettingsScope`/`useSessions` 合并改从 `dsh-api-session-controller`（crazx 别名）、`dsh-client-ui-settings`、`dsh-client-ui-session`、`dsh-client-ui-renderer` 进口，devDeps 全量钉 0.1.2-alpha.3 基线含类型闭包（防 pnpm 自动 peer 拖入 rc.8 旧副本致类型静默塌缩）；0.2.0-rc.6 起 client 入口 inject 补声明 `remote.session` 点号子路径——cordis 4 把每个 Remote namespace 挂成独立服务 `remote.<namespace>`，`ctx.remote.session` 经 traceable 代理回读 `ctx['remote.session']` 时只沿声明过该名的 fiber 链解析，未声明即 apply 抛 `without inject`（stock session-controller/ui-plan 同款声明；typecheck 对此失明，`Context['remote']` 类型把 session 声明成普通属性）；0.2.0-rc.7 起承接会话继承来源会话的模型选择——authorize 读 `modelSelection` 投影（`pending ?? lastUsed`）经 `llm.resolveCallConfig` 验证后钉在 Link（路由不可用即 `source-model-unavailable` fail loud，绝不静默降级默认模型），activate 在 final pristine check 与首次 inbox mutation 之间的同步窗口向目标 append `model/selection` 事件（**刻意不走 `selectModel`**——该链会 `saveSelection` 重写全局默认模型并写 effort 记忆，继承没有用户手势语义）；`model`/`fold.models` 带 default 向后兼容存量 Link；决策见 `docs/notes/2026-08-21-session-thread.md`、`docs/notes/2026-09-02-thread-desktop-owned.md`、`docs/notes/2026-09-02-thread-zod-inline.md`、`docs/notes/2026-09-02-packaged-profile-smoke.md`、`docs/notes/2026-09-03-thread-remote-session-create.md`、`docs/notes/2026-09-03-thread-remote-session-inject.md` 与 `docs/notes/2026-09-03-thread-model-inheritance.md`；0.3.0-rc.1 起 saga 正交 checkpoint 重构（标题契约/目标指纹/语义 pristine/恢复动作/存量迁移，见 `docs/notes/2026-09-10-thread-title-saga-checkpoints.md`））：prepare 对随包插件分别 typecheck/test/build、记录 tarball hash，并额外把 Web Search / model-image-input / send-while-running / model-efforts-editor / thread 的版本写进 revision manifest；壳首启原子解压并把随包插件作为同一事务幂等 `plugin add` 到 web Profile。层次压缩的根 patch 刻意为空，安装只保证用户 preset 可在隔离 `compaction` realm 解析该 Provider，不替用户改 shipped/default preset；Web Search 插件持久化 home patch，并以 Host assembly/guard 对所有 Agent Preset 执行开关策略。随 `.app` 发货的插件在自己的 `package.json` 标 `dsh.desktop.ship: true`（可选 `tarball` / `dest` / `env` / `pin`；`pin` 一经声明必须与包 `version` 严格相等，插件升版时同步改——`shipped-plugins.mjs` 对失配 fail loud，release 构建直接中止）；prepare、壳解压、`plugin add`、CI 检查与 packaged `--dump-config` 冒烟都扫这个字段。新插件不要改 workflow。独立插件 tag 不能替代 desktop Release。
