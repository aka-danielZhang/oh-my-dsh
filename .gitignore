# Node
node_modules/
dist/
out/

# dsh-desktop-bridge：类型解析锚（指向 DSH checkout 的符号链接）与构建产物
plugin/dsh-desktop-bridge/dsh
plugin/dsh-desktop-bridge/lib/
# dsh-reasoning-efforts：同款类型锚与构建产物
plugin/dsh-reasoning-efforts/dsh
plugin/dsh-reasoning-efforts/lib/
# dsh-web-search-toggle：构建产物（锚走 plugin/deepseek-harness 共享）
plugin/dsh-web-search-toggle/lib/
# dsh-branding：构建产物（依赖全钉 registry，无锚）
plugin/dsh-branding/lib/
# dsh-compaction-hierarchical：host-only 构建产物
plugin/dsh-compaction-hierarchical/lib/
# dsh-fs-observation-log：host-only 构建产物（依赖全钉 registry，无锚）
plugin/dsh-fs-observation-log/lib/
# dsh-mcp-settings 的同级 harness 锚（scripts/setup-plugins.mjs 再生）
plugin/deepseek-harness

# 打包资源（prepare-desktop-bundle 再生：runtime/bridge tarball + revision 副本）
src/resources/
dist-electron/
release/

# 签名与公证凭据（绝不入库）
*.p8
*.p12
*.pem
*.cer
*.certSigningRequest
*.mobileprovision
*.key
*.pfx
AuthKey_*

# Logs & local
*.log
.DS_Store
.env
.env.*

# Editor
.idea/
.vscode/
*.swp

# 运行时组装缓存（fork 克隆与构建产物，机器本地再生）
runtime/src/
runtime/build/

# fork 包 tarball（prepare-runtime 再生）
runtime/tarballs/

# dsh-model-image-input：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-model-image-input/lib/

# 沙箱会话的 pnpm 本地 store 回退（全局 store 不可写时 pnpm 落在仓根，机器本地再生）
.pnpm-store/

# dsh-send-while-running：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-send-while-running/lib/

# dsh-model-efforts-editor：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-model-efforts-editor/lib/

# dsh-question-rail：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-question-rail/lib/

# dsh-thread：构建产物
plugin/dsh-thread/lib/

# dsh-ohmymemo：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-ohmymemo/lib/

# dsh-usage-stats：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-usage-stats/lib/

# dsh-copy-session-id：构建产物（scripts/new-plugin.mjs 生成）
plugin/dsh-copy-session-id/lib/
